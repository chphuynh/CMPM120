function makeGridCollision(gridObstacle) {
	for(var i = 0; i < 60; i += gridWidth){
		for(var j = 0; j < 70; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 60; i < 90; i += gridWidth){
		for(var j = 0; j < 50; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 90; i < 140; i += gridWidth){
		for(var j = 0; j < 30; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 140; i < 210; i += gridWidth){
		for(var j = 0; j < 50; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 210; i < 220; i += gridWidth){
		for(var j = 0; j < 70; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 220; i < 280; i += gridWidth){
		for(var j = 0; j < 90; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 280; i < 300; i += gridWidth){
		for(var j = 0; j < 70; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 300; i < 320; i += gridWidth){
		for(var j = 0; j < 50; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 320; i < 330; i += gridWidth){
		for(var j = 0; j < 30; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 330; i < 360; i += gridWidth){
		for(var j = 0; j < 10; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 360; i < 380; i += gridWidth){
		for(var j = 0; j < 30; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 380; i < 400; i += gridWidth){
		for(var j = 0; j < 50; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 400; i < 470; i += gridWidth){
		for(var j = 0; j < 90; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 470; i < 520; i += gridWidth){
		for(var j = 0; j < 70; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 520; i < 540; i += gridWidth){
		for(var j = 0; j < 50; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 540; i < 590; i += gridWidth){
		for(var j = 0; j < 30; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 590; i < 600; i += gridWidth){
		for(var j = 0; j < 50; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 0; i < 20; i += gridWidth){
		for(var j = 340; j < 400; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 20; i < 40; i += gridWidth){
		for(var j = 360; j < 400; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 40; i < 70; i += gridWidth){
		for(var j = 380; j < 400; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 110; i < 140; i += gridWidth){
		for(var j = 380; j < 400; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 140; i < 210; i += gridWidth){
		for(var j = 360; j < 400; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 210; i < 470; i += gridWidth){
		for(var j = 380; j < 400; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 260; i < 410; i += gridWidth){
		for(var j = 360; j < 380; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 310; i < 400; i += gridWidth){
		for(var j = 340; j < 360; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 330; i < 380; i += gridWidth){
		for(var j = 320; j < 340; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 500; i < 530; i += gridWidth){
		for(var j = 380; j < 400; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
	for(var i = 530; i < 600; i += gridWidth){
		for(var j = 360; j < 400; j += gridWidth){
			gridObstacle.push({x:i,y:j});
		}
	}
}